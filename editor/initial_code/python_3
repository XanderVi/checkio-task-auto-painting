def painting(capacity, number):
    return ""


if __name__ == '__main__':
    #This part is using only for self-checking and not necessary for auto-testing

    assert painting(2, 3) == "01,12,02"
    assert painting(6, 3) == "012,012"
    assert painting(3, 6) == "012,012,345,345"
